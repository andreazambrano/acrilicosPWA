import { ThrottleOptions } from './throttle-options.interface';
/**
 * Throttle decorator
 *
 *  class MyClass {
 *    throttleable(10)
 *    myFn() { ... }
 *  }
 */
export declare function throttleable(duration: number, options?: ThrottleOptions): (_: any, key: string, descriptor: any) => {
    configurable: boolean;
    enumerable: any;
    get: () => any;
};
