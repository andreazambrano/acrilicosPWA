import { OnChanges } from '@angular/core';
import { DefaultFlexDirective } from '@angular/flex-layout';
export declare class SplitAreaDirective implements OnChanges {
    readonly flexDirective: DefaultFlexDirective;
    minBasis: string;
    maxBasis: string;
    fxFlex: string;
    overflow: string;
    initialFlexBasis: string[];
    currentFlexBasis: string[];
    readonly fxFlexFill: boolean;
    constructor(flexDirective: DefaultFlexDirective);
    ngOnChanges(): void;
    updateStyle(flexBasis?: string | number): void;
    isPercent(basis?: string): boolean;
    private getCurrentFlexParts;
}
