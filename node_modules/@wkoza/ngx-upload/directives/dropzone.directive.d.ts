import { ElementRef, OnInit, Renderer2, Injector } from '@angular/core';
import { FormGroupDirective, NgForm } from '@angular/forms';
import { DropTargetOptions } from '../utils/configuration.model';
import { NgxUploadLogger } from '../utils/logger.model';
import { HttpClientUploadService } from '../services/httpClientUpload.service';
/**
 * Transforms a node into a drag and drop zone.
 */
export declare class NgxDragAndDropDirective implements OnInit {
    private el;
    private renderer;
    private injector;
    private logger;
    uploader: HttpClientUploadService;
    private dropOptions;
    private ngForm;
    private formGroupDirective;
    ngxDragAndDrop: DropTargetOptions;
    private readonly formGroup;
    constructor(el: ElementRef, renderer: Renderer2, injector: Injector, logger: NgxUploadLogger, uploader: HttpClientUploadService, dropOptions: DropTargetOptions, ngForm: NgForm, formGroupDirective: FormGroupDirective, platformId: Object);
    ngOnInit(): void;
    onDragLeave(event: any): void;
    dropEvent(event: any): void;
    onDragOver(event: any): void;
    private stopAndPrevent;
    private getTransfer;
    private haveFiles;
}
