import { AfterViewInit, Injector, Renderer2 } from '@angular/core';
import { FormGroupDirective, NgForm } from '@angular/forms';
import { InputFileOptions } from '../utils/configuration.model';
import { HttpClientUploadService } from '../services/httpClientUpload.service';
export declare class InputfileComponent implements AfterViewInit {
    private injector;
    private uploader;
    private renderer;
    private ngForm;
    private formGroupDirective;
    file: any;
    files: Set<File>;
    options: InputFileOptions;
    private formGroup;
    constructor(injector: Injector, uploader: HttpClientUploadService, renderer: Renderer2, ngForm: NgForm, formGroupDirective: FormGroupDirective);
    onFilesAdded(): void;
    ngAfterViewInit(): void;
}
